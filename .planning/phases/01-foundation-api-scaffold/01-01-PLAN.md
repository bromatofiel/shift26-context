---
phase: 01-foundation-api-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - frontend/package.json
  - backend/package.json
  - shared/package.json
  - shared/tsconfig.json
  - shared/src/types/analysis.ts
  - shared/src/index.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Monorepo structure exists with frontend/, backend/, shared/ directories"
    - "Shared types can be imported by both frontend and backend"
    - "TypeScript compilation works across all packages"
  artifacts:
    - path: "shared/src/types/analysis.ts"
      provides: "Analysis response schema types"
      exports: ["AnalysisRequest", "AnalysisResponse", "BiasScore", "CounterPerspective"]
    - path: "shared/src/index.ts"
      provides: "Shared package exports"
      exports: ["all types from analysis.ts"]
    - path: "package.json"
      provides: "Workspace configuration"
      contains: "workspaces"
  key_links:
    - from: "backend/package.json"
      to: "shared/"
      via: "workspace dependency"
      pattern: "\"@blindspot/shared\""
    - from: "frontend/package.json"
      to: "shared/"
      via: "workspace dependency"
      pattern: "\"@blindspot/shared\""
---

<objective>
Create monorepo structure with shared TypeScript types for API contract.

Purpose: Establish project foundation with type-safe communication between frontend and backend. Per decisions D-01 and D-02, we need a monorepo with shared types to ensure schema consistency.

Output: Working monorepo with compilable shared types package that both frontend and backend can import.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-api-scaffold/01-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create monorepo structure</name>
  <read_first>
    - .planning/phases/01-foundation-api-scaffold/01-CONTEXT.md (decisions D-01, D-02)
  </read_first>
  <files>
    package.json
    tsconfig.json
    frontend/package.json
    backend/package.json
    shared/package.json
    shared/tsconfig.json
  </files>
  <action>
    Initialize npm workspaces monorepo with three packages:

    1. Root package.json with workspaces: ["frontend", "backend", "shared"]
    2. Create frontend/ directory with package.json (name: "@blindspot/frontend")
    3. Create backend/ directory with package.json (name: "@blindspot/backend")
    4. Create shared/ directory with package.json (name: "@blindspot/shared")

    Backend dependencies: Add hono@^4.0.0, @hono/zod-validator@^0.2.0, zod@^3.22.0
    Shared dependencies: Add zod@^3.22.0 (for schema definitions)
    All packages: Add typescript@^5.3.0, @types/node@^20.0.0 as devDependencies

    TypeScript configuration:
    - Root tsconfig.json with base config (strict: true, esModuleInterop: true, skipLibCheck: true)
    - shared/tsconfig.json extending root, outDir: "./dist", declaration: true
    - Backend and frontend will extend root config in next plan

    Use npm as package manager per Claude's discretion.
  </action>
  <verify>
    <automated>npm install && ls frontend/ backend/ shared/ && cat shared/package.json | grep "@blindspot/shared"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "workspaces": ["frontend", "backend", "shared"]
    - frontend/package.json has name "@blindspot/frontend"
    - backend/package.json has name "@blindspot/backend"
    - shared/package.json has name "@blindspot/shared"
    - backend/package.json dependencies include "hono", "@hono/zod-validator", "zod"
    - shared/package.json dependencies include "zod"
    - shared/tsconfig.json has "declaration": true
    - npm install completes without errors
  </acceptance_criteria>
  <done>Three workspace packages created, dependencies installed, TypeScript configured for compilation</done>
</task>

<task type="auto">
  <name>Task 2: Define shared analysis types</name>
  <read_first>
    - .planning/REQUIREMENTS.md (ANA-01 through ANA-05 for schema structure)
    - .planning/phases/01-foundation-api-scaffold/01-CONTEXT.md (decisions D-05, D-08)
  </read_first>
  <files>
    shared/src/types/analysis.ts
    shared/src/index.ts
  </files>
  <action>
    Create TypeScript types representing the complete API contract per requirements ANA-01 to ANA-05:

    File: shared/src/types/analysis.ts

    1. BiasScore type:
       - score: number (0-10 per ANA-01)
       - color: "green" | "orange" | "red" (per ANA-01)
       - confidence: number (0-1)

    2. Signal type:
       - type: "tone" | "framing" | "omission" | "source_selection" (per ANA-02)
       - description: string
       - severity: "low" | "medium" | "high"

    3. CounterPerspective type:
       - title: string
       - source: string
       - url: string
       - key_differences: string[] (per ANA-03)
       - published_date?: string

    4. SourceArticle type:
       - url: string
       - title: string
       - content: string
       - extracted_date?: string
       - media_url?: string

    5. GlobalContext type:
       - summary: string (per ANA-04)
       - missing_angles: string[]

    6. AnalysisRequest type (per D-08):
       - url: string
       - locale?: string
       - timeout_ms?: number

    7. AnalysisResponse type (combines all above):
       - source_article: SourceArticle
       - bias_score: BiasScore
       - main_signals: Signal[]
       - counter_perspectives: CounterPerspective[]
       - global_context: GlobalContext
       - analyzed_at: string (ISO timestamp)

    8. HealthResponse type (per D-07):
       - status: "ok"
       - version: string
       - uptime_ms: number

    File: shared/src/index.ts
    Export all types from analysis.ts using: export * from './types/analysis.js'

    All types should be plain TypeScript interfaces (not Zod schemas - those will be in backend).
  </action>
  <verify>
    <automated>npx tsc --project shared/tsconfig.json && ls shared/dist/types/analysis.d.ts && cat shared/src/index.ts | grep "export \* from"</automated>
  </verify>
  <acceptance_criteria>
    - shared/src/types/analysis.ts contains interfaces: BiasScore, Signal, CounterPerspective, SourceArticle, GlobalContext, AnalysisRequest, AnalysisResponse, HealthResponse
    - BiasScore has color field with union type "green" | "orange" | "red"
    - Signal has type field with union including "tone", "framing", "omission"
    - CounterPerspective has key_differences as string[]
    - AnalysisResponse includes all required nested types
    - shared/src/index.ts exports all types
    - TypeScript compilation succeeds and generates .d.ts files in shared/dist/
  </acceptance_criteria>
  <done>Complete type definitions exist matching ANA requirements, compiled successfully, ready for import by backend and frontend</done>
</task>

</tasks>

<verification>
After plan completion:

1. Monorepo structure verification:
   ```bash
   npm ls --workspaces
   ```
   Should show @blindspot/frontend, @blindspot/backend, @blindspot/shared

2. Type compilation verification:
   ```bash
   npx tsc --project shared/tsconfig.json --noEmit
   ```
   Should succeed with no errors

3. Type exports verification:
   ```bash
   cat shared/dist/index.d.ts
   ```
   Should contain exported type declarations
</verification>

<success_criteria>
Plan succeeds when:
1. npm install completes successfully at root
2. All three workspace packages (frontend, backend, shared) are recognized by npm
3. shared/src/types/analysis.ts contains all 8 required types
4. TypeScript compilation of shared package produces .d.ts declaration files
5. Backend and frontend packages can reference @blindspot/shared in their package.json
6. No TypeScript errors in shared package
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-api-scaffold/01-01-SUMMARY.md` documenting:
- Monorepo structure created
- Type definitions established (list all 8 types)
- Package manager choice (npm)
- TypeScript configuration approach
- Any decisions made (folder structure, naming conventions)
</output>
